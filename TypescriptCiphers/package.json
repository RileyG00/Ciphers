{
	"name": "substitution-ciphers",
	"version": "1.0.0",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [ "dist" ],
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "tsc --build",
		"clean": "tsc --build --clean"
	},
	"keywords": [ "cipher", "encryption", "typescript", "substitution", "griffinere" ],
	"author": "Your Name",
	"license": "MIT",
	"type": "module",
	"description": "A TypeScript implementation of various substitution ciphers.",
	"devDependencies": {
		"@types/node": "^24.1.0",
		"eslint": "^9.31.0",
		"typescript": "^5.8.3"
	}
}
